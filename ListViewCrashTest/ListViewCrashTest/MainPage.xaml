<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ListViewCrashTest"
             x:Class="ListViewCrashTest.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackLayout Orientation="Horizontal" Margin="10">
            <Label Text="Search: "/>
            <Editor Text="{Binding SearchText}" HorizontalOptions="Fill" HeightRequest="40" />
        </StackLayout> 

        <ActivityIndicator WidthRequest="32" HeightRequest="32" IsRunning="True" Color="SteelBlue" IsVisible="{Binding IsWorking}" Grid.Row="1" />

        <ListView Grid.Row="2" ItemsSource="{Binding Items}" CachingStrategy="RecycleElement">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button HorizontalOptions="Center" Text="Load" Grid.Row="3" Command="{Binding LoadCommand}"/>
    </Grid>

</ContentPage>
